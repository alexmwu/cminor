// Test case 11
// Nested if statement

main: function integer () = {
  if (true) {
    print "true branch correctly taken", '\n';
    if (false) {
      print "INCORRECT: true branch taken", '\n';
    } else {
      print "else branch correctly taken", '\n';
    }
  } else {
    print "INCORRECT: else branch taken", '\n';
  }

  if (false) {
    print "INCORRECT: true branch taken", '\n';
  } else {
    print "else branch correctly taken", '\n';
    if (true) {
      print "true branch correctly taken", '\n';
    } else {
      print "INCORRECT: else branch taken", '\n';
    }
  }

  return 0;
}
